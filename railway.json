{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd core && mvn clean package -DskipTests",
    "startCommand": "cd core && java -Dserver.port=$PORT -jar target/demo-0.0.1-SNAPSHOT.jar",
    "nixpacks": {
      "provider": "maven",
      "projectDir": "core"
    }
  },
  "deploy": {
    "runtime": "V2"
  }
}
